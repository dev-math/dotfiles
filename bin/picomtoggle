#!/bin/env bash
if [[ $1 = refresh ]]
then
	if pgrep -x "picom" > /dev/null
	then
		echo 
	else
		echo 
	fi
else
	if pgrep -x "picom" > /dev/null
	then
		killall picom
		polybar-msg hook beautiful-toggle 1
	else
		polybar-msg hook beautiful-toggle 2
		picom
	fi
fi
