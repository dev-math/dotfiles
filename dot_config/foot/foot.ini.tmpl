#term=foot # (or xterm-256color if built with -Dterminfo=disabled)

font={{ .font }}:size=16, Noto Color Emoji:size=16

[scrollback]
lines=10000

[colors]
alpha=0.9

[main]
include=~/.cache/wal/foot.ini

# vim: ft=dosini
